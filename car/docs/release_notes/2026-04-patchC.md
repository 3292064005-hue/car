Audience: releasers / auditors / reviewers
Scope: governance closure for P0/P1/P2 implementation, compatibility notes, validation scope, and non-claims
Source of truth: implemented code changes and attached validation evidence for this package
Status: release-note

# 2026-04 Patch C Release Notes

本次交付完成的是治理与接入语义收口，不是新增业务功能面。

## 本次关闭项

### GOV-P0-01 repository boundary closure
- 将仓库正式定义为 `Ubuntu authoritative runtime + board-boundary contract / harness repository`
- 明确仓内 / 仓外主链路闭环止点
- 统一 9100 / 9001 的权威语义

### GOV-P0-02 feature admission
- 新增功能准入注册表
- 新增准入校验脚本
- 将 authority / config / tests / acceptance / rollback 纳入 feature registry
- 前端默认可见功能消费 governance contract 并显示成熟度 / 证据标签，registry 的 `uiConsumers` 与前端组件绑定已纳入自动校验；若组件目录存在但未提取到任何绑定则会直接报错

### GOV-P0-03 target-acceptance governance
- 默认硬件运行语义改为 `ros_soft_driver`，不携带板级执行确认；只有显式 `verified_board_driver` 且绑定新鲜 HIL / target acceptance 证据时才允许板级执行声明
- release gate 增加治理类报告产出

### GOV-P1-01 authoritative write path tightening
- 前端发送策略新增只读会话硬拒绝
- 连接到 9001 observer 面时，所有写命令被浏览器侧直接拦截

### GOV-P1-02 lane lifecycle visibility closure
- lane registry 增加 lifecycle stage、default exposure、retention / exit condition
- Reports 页默认只展示 `default_visible` 的主线 lane，实验 / rollback-only lane 需要显式展开

### GOV-P1-03 capability ownership matrix
- 新增能力所有权与 evidence layering 文档/报告

### GOV-P2-01 replay evidence split
- 明确前端本地 replay 与系统级 replay 证据分层

### GOV-P2-02 report layering
- 报告信号分层显式化，`runtime_supervision` 之外的 report 默认不是 machine gate

### GOV-P2-03 runtime topology manifest
- 新增统一运行拓扑清单报告

## 兼容说明

- 9100 仍是主线接入面，但现在文档与前端策略都将其定义为唯一写入口。
- 9001 保留为只读观测面；任何旧“fallback write”理解都应废弃。
- legacy monolith lane 仍保留，但仅用于显式 rollback-only 场景。

## 本次交付未声明

- HIL acceptance 可自动等价为任意目标环境都已验收，或可在没有 HIL 执行报告输入时由打包流程自生成成功结论
- target-environment acceptance 已在本包内完成
- `ros_soft_driver` 等价于已验板级执行闭环
- Nav2 lane 已升级为运行完成态主线能力
