Audience: releasers / auditors / reviewers
Scope: governed nav2 adapter lane truth correction, evidence binding hardening, and governance registry convergence
Source of truth: implemented code changes and attached validation evidence for this package
Status: release-note

# 2026-04 Patch D Release Notes

## 本次关闭项

### GOV-P0-01 nav2 adapter truth correction (gated)
- `nav2_provider` 保持实验隔离 lane，但其实现状态已明确收紧为 **governed local adapter runtime**
- adapter lane 继续提供 route / cancel / health / recovery / backend selection 语义
- 当前包内默认只实现 `local_adapter`；只有同时满足“外部栈可用 + 本包明确声明 backend 已接入”时，才允许对外宣称 `external_nav2_stack` 已集成
- `simple_nav_provider` 仍保持默认主线与回滚路径
- 运行入口默认仍会拒绝 experimental lane，只有显式设置 `ROBOT_ALLOW_EXPERIMENTAL_NAVIGATION_PROVIDER=1` 时才允许切换
- `simulation_smoke / host_harness_smoke / provider_switch_smoke / operator_docs_review / target_environment_acceptance` 五类工件继续绑定到运行入口 gate
- 当配置声明 external backend 已接入时，运行入口会额外要求 `external_backend_smoke`
- 任一必需工件缺失或无效时，运行入口自动回退 `simple_nav_provider`

### GOV-P0-02 evidence binding hardening
- `verificationIdentity.sourceReleaseIdentity` 现在强绑定 `workspaceId / artifactId / sourceTreeSha256 / layoutMode / workspaceManifestSha256`
- `artifacts/validation/VALIDATION_EVIDENCE.md` 改为使用可移植 `<canonical-root>` 占位符，而不是携带陈旧绝对路径
- 新增 `scripts/check_validation_evidence_binding.py` 与 evidence 层校验联动，阻断旧工作区路径复用

### GOV-P0-03 capability single-source convergence
- 新增 `robot_contracts.capability_registry` 作为能力真值单源
- `provider_contract`、lane 描述、feature admission 成熟度推导、frontend governance 生成物统一收敛到 capability registry
- release note 现改为显式区分：**lane 已治理可激活** 与 **外部 Nav2 后端已真实集成**

### GOV-P1-01 hardware standardization groundwork
- 保留 projection-only / direct-driver 双 lane 治理主线
- 新增 `standardizationTarget` contract，明确未来 `ros2_control system_interface` 的目标模型
- 本次未把仓库伪装成已完成 `ros2_control` 接入；后续标准化迁移仍需独立实施与验收

### GOV-P1-02 standard read-only bridge runtime
- 新增 `standard_observability_contract.py`、`standard_observability_bridge_launcher.py` 与 `standard_observability_bridge_runtime.py`
- `bridge.yaml`、`launch_common.py`、`resolve_runtime_surface_config.py` 已接入标准只读桥 contract / 配置 / 运行入口
- 运行时只允许 `repo_readonly_websocket` 家族，并仅监听 localhost
- 客户端只允许 `ping / list_topics / subscribe / unsubscribe` 四类只读操作，所有其它操作都会被拒绝
- 运行时仅代理 9001 observer surface 的只读主题，不进入 9100 authority path，也不透传 publish/service/action 写能力

### GOV-P1-03 implementation-alignment gate
- 新增 `scripts/check_lane_implementation_alignment.py`
- 新增 `scripts/check_capability_registry_consistency.py`
- 新增 `scripts/render_nav2_external_backend_smoke.py`
- release/common checks 现在不仅校验 contract 自洽，也会校验“声明是否强于实现”，并在需要时要求 `external_backend_smoke`

### GOV-P2-01 fleet adapter boundary groundwork
- 新增 `fleet_adapter_boundary.py` 与 `fleet_adapter.yaml`
- runtime surface 已导出 `fleetAdapterBoundaryContract`，用于单机 runtime 与未来调度层之间的边界说明
- 当前默认关闭，不改变现有单机 authority split

### GOV-P2-02 source release strict-by-default
- `scripts/package_source_release.py` 默认严格拒绝 source-tree build/dist 污染
- `--allow-source-tree-artifacts` 仅保留给显式本地调试场景，并要求 `INSPECTION_ROBOT_LOCAL_DEBUG=1`
- CI 已切换为默认严格模式

## 本次交付未声明

- 外部 Nav2 栈已在当前包内完成 action / planner / controller / recovery server 接入
- `external_nav2_stack_available=true` 即可代表本包已具备外部 Nav2 backend integration
- 未提供 `external_backend_smoke` 时即可宣称当前包已完成外部 Nav2 backend 集成
- 未提供 hardware_in_loop_acceptance 时即可宣称 direct-driver 已成为板级默认权威
- 浏览器离线回放可替代系统级证据包
